export class UserLoginData {

    constructor (
        usernameOrEmail: string,
        password: string
    ) {
        usernameOrEmail = usernameOrEmail;
        password = password;
    }

    usernameOrEmail: string;
    password: string;
}